---
version: '3'
    
services:
          
  morph-server:
    build: git@github.com:tarunnsingh/morph-server.git
    ports:
      - 5000

  morph-client:
    build: .
    hostname: morph-client
    links:
      - morph-server
